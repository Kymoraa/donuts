import 'package:flutter/material.dart';
class SmoothiePage extends StatelessWidget {
  const SmoothiePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Smoothies'),
    );
  }
}
